= render "shared/head"
		
.site-wrapper
	.site-wrapper-inner
		.cover-container
			= render "shared/notice_and_alert"
			.masthead.clearfix
				.inner
					%h3.masthead-brand
						= "Bring Me That"
					%nav
						%ul.nav.masthead-nav
							%li.active 
								%a{:href => "#"}Home

						
		= yield
